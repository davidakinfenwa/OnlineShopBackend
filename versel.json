{
    "builds": [{
        "src": "OnlineShopBackend/storeapi/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "OnlineShopBackend/storeapi/wsgi.py"
        }
    ]
}